
1. **Módulo de Algoritmos** (`server/algoritmos`)
   - Busca sequencial (O(N))
   - Busca binária (O(log N)) — requer array ordenado
   - Bubble Sort (O(N^2))
   - Selection Sort (O(N^2))
   - Quick Sort (O(N log N) típico)
   - Função de benchmark que mede tempo real (modelo experimental) usando `process.hrtime()`

2. **POO aplicada** (`server/models`)
   - `Produto` (encapsulamento: atributos privados, getters/setters, métodos de estoque)
   - `Estoque` (herda `Produto` e sobrescreve comportamento: exemplo de polimorfismo)

3. **Rotas Express** (`server/routers/algoritmos.js`)
   - `/algoritmos` — index com formulários
   - `/algoritmos/busca-sequencial?nome=...`
   - `/algoritmos/busca-binaria?nome=...`
   - `/algoritmos/ordenar?algoritmo=quick|bubble|selection`
   - `/algoritmos/benchmark?n=1000`

4. **Views EJS** para visualizar resultados e tempos.

1. Acesse: `http://localhost:PORT/algoritmos`
2. Rode as buscas: então mostre número de operações (ops) e tempo (ms).
3. Rode ordenações e compare tempos entre algoritmos.
4. Rode `benchmark` para N=1000, 10000, 100000 e comente o crescimento do tempo (modelo experimental).
5. Mostre as classes `Produto` e `Estoque` e explique:
   - Encapsulamento (atributos privados)
   - Herança (Estoque extends Produto)
   - Polimorfismo (removerEstoque sobrescrito)

## Pontos que conectam com a matéria
- Modelagem com POO aqui aplicado a Produtos/Estoque)
- Análise de algoritmos — Big-O
- Comparação prática entre algoritmos via benchmark (modelo experimental)
